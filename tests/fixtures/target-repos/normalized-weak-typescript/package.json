{
  "name": "krn-fixture-normalized-weak-typescript",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "rm -rf .tmp-test && tsc -p tsconfig.json --outDir .tmp-test --noEmit false && node .tmp-test/tests/userService.test.js && rm -rf .tmp-test"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
