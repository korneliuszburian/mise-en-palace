{
  "name": "krn-fixture-temporal-policy-drift-typescript",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "rm -rf .tmp-test && tsc -p tsconfig.json --outDir .tmp-test --noEmit false && node .tmp-test/tests/payoutPolicy.test.js && rm -rf .tmp-test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
