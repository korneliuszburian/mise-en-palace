# Async Job Boundary Target

Repair only the job boundary files named by the task. Do not add a scheduler,
daemon, dashboard, queue service, or parent KRN source dependency.

Expected owner files:

- `docs/job-contract.md`
- `src/jobQueue.ts`
- `tests/jobQueue.test.ts`
