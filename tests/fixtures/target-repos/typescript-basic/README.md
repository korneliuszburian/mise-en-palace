# TypeScript Basic Fixture

Small target repo fixture for KRN target planning and harness smoke checks.

It intentionally contains source, test, docs, secret-shaped, and generated
runtime-shaped paths so KRN can prove project-scoped source seeds and trust
exclusions without using a real external repository.

