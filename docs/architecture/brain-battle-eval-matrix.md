# Brain Battle Eval Matrix

Status: bounded eval matrix for KRN brain-in-battle checks.

This is not a broad eval platform. It classifies dogfood-derived KRN behavior
boundaries into deterministic guards, candidate checks, real-trial evidence
needs, and rejected eval theater.

## Classification

| Check | Status | Guard | Evidence | Does not prove |
|---|---|---|---|---|
| Local TypeScript target fixture exposes realistic source seeds and trust exclusions for target init/connect/plan behavior. | implemented now | `pnpm eval:brain-battle:smoke` executes `golden-case-target-fixture-battle-001-a`; `pnpm db:smoke:target-repo-harness` readbacks fixture source seeds and trust exclusions. | `tests/fixtures/target-repos/typescript-basic/AGENTS.md`; `packages/harness/src/goldenKrnBehaviorGate.ts`; `packages/cli/src/targetRepoHarnessSmoke.ts`; `docs/reviews/controlled-dogfood/2026-06-26-target-fixture-battle-harness/REPORT.md`. | Does not prove a real external operator target trial or exact owner-file recall below named roots. |
| Target owner-file recall can surface bounded files below named target roots when the read model provides explicit owner-file signals. | implemented now | `pnpm eval:brain-battle:smoke` executes `golden-case-target-owner-file-below-roots-001-a`; harness compiler tests verify `compileHarnessPlan` includes `tests/readiness.test.ts`. | `packages/harness/src/activation/ownerFileRecall.ts`; `packages/harness/src/compiler/index.test.ts`; `packages/harness/src/goldenKrnBehaviorGate.ts`; `docs/reviews/controlled-dogfood/2026-06-26-target-owner-file-recall-below-roots/REPORT.md`. | Does not prove filesystem crawling, broad arbitrary repo inference, or external target operator readiness. |
| Target-like evidence capture can be read back with proof/non-proof boundaries and no Memory Core mutation. | implemented now | `pnpm db:smoke:target-repo-harness` creates evidence/review/feedback records, readbacks them through the repository aggregate, and checks weak default command proof. | `packages/cli/src/targetRepoHarnessSmoke.ts`; `packages/cli/src/targetRepoHarnessSmoke.test.ts`; `docs/reviews/controlled-dogfood/2026-06-26-target-evidence-readback-loop/REPORT.md`. | Does not prove commands executed, review correctness, memory usefulness, or external target readiness. |
| Target-like memory usefulness can be measured with explicit helped feedback without automatic memory promotion. | implemented now | `pnpm db:smoke:target-repo-harness` seeds a smoke-scoped MemoryRecord, verifies it is included, records `MemoryApplication outcome=helped`, readbacks positive feedback, and reports automatic mutation none. | `packages/cli/src/targetRepoHarnessSmoke.ts`; `packages/cli/src/targetRepoHarnessSmoke.test.ts`; `docs/reviews/controlled-dogfood/2026-06-26-target-memory-usefulness-loop/REPORT.md`. | Does not prove memory usefulness on arbitrary target repos or that future memory should be promoted automatically. |
| Target activation uses target read-model source seeds and trust exclusions instead of stale KRN static owner-file recall for target projects. | implemented now | `pnpm eval:brain-battle:smoke` executes `goldenKrnBehaviorGate` target trust exclusion case. | `packages/harness/src/goldenKrnBehaviorGate.ts`; `packages/harness/src/goldenKrnBehaviorGate.test.ts`; `docs/reviews/controlled-dogfood/2026-06-25-target-activation-read-model/REPORT.md`. | Does not prove exact owner-file recall below target source roots. |
| Trust exclusions cover `.env*`, `.git/`, `node_modules/`, `.muke/`, `.supersearch/runtime/`, `dist/`, and `build/`. | implemented now | Same deterministic GoldenGate case checks the target trust exclusion candidate metadata. | `packages/harness/src/activation/ownerFileRecall.ts`; `packages/harness/src/goldenKrnBehaviorGate.ts`. | Does not prove every target repo generated/runtime folder is known. |
| Codex execution brief includes objective, constraints, non-goals, context inclusions/exclusions, untrusted-context warnings, evidence contract, review burden, rollback, and proof boundaries. | implemented now | `pnpm eval:brain-battle:smoke` runs Codex adapter golden behavior for the brief contract; focused adapter tests cover untrusted-context warning rendering. | `tests/fixtures/golden-tasks/codex-brief-behavior.json`; `packages/codex-adapter/src/codexBriefGoldenBehavior.test.ts`; `packages/codex-adapter/src/renderExecutionBrief.test.ts`; `docs/reviews/controlled-dogfood/2026-06-25-codex-brief-contract-hardening/REPORT.md`. | Does not prove Codex follows the brief or prompt-injection resistance. |
| Context assembly rejects broad dumps and preserves selected-context `expectedUse` plus explicit exclusion reasons. | implemented now | `pnpm eval:brain-battle:smoke` executes GoldenGate ContextROI behavior and harness activation tests. | `packages/harness/src/goldenKrnBehaviorGate.ts`; `packages/harness/src/goldenKrnBehaviorGate.test.ts`; `packages/harness/src/activation/contextRoi.ts`; `tests/fixtures/golden-tasks/boundary-behavior.json`. | Does not prove context scoring is optimal for every target repo. |
| SourceClaim and SourceDecision records require mechanism, KRN implication, consumer, falsifier or `doesNotProve`, and reject decorative source retention. | implemented now | `pnpm eval:brain-battle:smoke` executes `goldenKrnBehaviorGate` decorative source rejection case. | `packages/core/src/source.ts`; `packages/harness/src/goldenKrnBehaviorGate.ts`; `docs/architecture/security-trust-boundaries.md`. | Does not prove source selection quality or that every decorative source is detected before operator review. |
| Source decisions and source rejections should not be type-compatible with retained source claims or each other. | implemented now | Core ID typecheck proof brands `SourceDecisionId` and `SourceRejectionId` and verifies they are not assignable to `SourceClaimId` or each other while preserving string compatibility. | `packages/core/src/ids.ts`; `packages/core/src/ids.typecheck.ts`; `docs/KRN_SOURCES.md#designing-your-types`; `docs/runbooks/pattern-intake.md`. | Does not prove source review judgment, source retrieval quality, or that every source-related ID alias should be branded. |
| Observation redaction handles target-like env/package output before truncation. | implemented now | Focused harness observer input tests cover credentialed URLs, API keys, npm tokens, cookies, and redaction-before-truncation. | `packages/harness/src/observations/observerInput.ts`; `packages/harness/src/observations/observerInput.test.ts`; `docs/architecture/security-trust-boundaries.md`. | Does not prove every secret format is known or that evidence metadata cannot carry operator text. |
| KRN lifecycle/state-dependent domain models should use narrow unions or discriminated unions when valid fields differ by state. | deferred after spot-check | V60 found the sampled high-risk domain surfaces already use narrow unions, Zod enums, or discriminated unions; keep as a standard/eval candidate and reopen only on repeated local drift. | `docs/KRN_SOURCES.md#unions-literals-and-narrowing`; `docs/runbooks/pattern-intake.md`; `docs/reviews/controlled-dogfood/2026-06-27-v59-first-pattern-intake-runbook-application/REPORT.md`; `docs/reviews/controlled-dogfood/2026-06-27-v60-typescript-lifecycle-union-drift-spot-check/REPORT.md`. | Does not prove exhaustive TypeScript quality, current source has no future drift, or that a broad type rewrite is valuable. |
| Production TypeScript package source must avoid unsafe casts, TS suppressions, explicit `any`, and unchecked `JSON.parse` boundaries. | implemented now | Focused TypeScript boundary invariant scans `packages/*/src/**/*.ts` production files, rejects `as any`, `as unknown as`, TS suppressions, explicit `any` annotations, and requires direct `JSON.parse` results to be assigned to `unknown`; `pnpm eval:brain-battle:smoke` runs the guard. | `packages/harness/src/typescriptBoundaryInvariants.test.ts`; `docs/standards/typescript-excellence.md`; `docs/KRN_SOURCES.md#ts-reset`. | Does not prove all type design is ideal, test fixtures are production-safe, or every external input boundary has perfect domain validation. |
| Anti-memory candidate and record IDs should not be type-compatible with memory candidate/record IDs. | implemented now | Core ID typecheck proof brands `AntiMemoryCandidateId` and `AntiMemoryRecordId` and verifies they are not assignable to the matching memory IDs. | `packages/core/src/ids.ts`; `packages/core/src/ids.typecheck.ts`; `docs/KRN_SOURCES.md#designing-your-types`. | Does not prove every ID alias should be branded or that runtime IDs changed. |
| Evidence, review, and feedback spine IDs should not be type-compatible with each other. | implemented now | Core ID typecheck proof brands `EvidenceBundleId`, `ReviewAssessmentId`, and `FeedbackDeltaId` and verifies they are not mutually assignable while preserving string compatibility. | `packages/core/src/ids.ts`; `packages/core/src/ids.typecheck.ts`; `docs/KRN_SOURCES.md#designing-your-types`. | Does not prove runtime IDs changed, every remaining ID alias should be branded, or evidence/review correctness. |
| Memory application feedback records helped/hurt/neutral/stale without direct Memory Core mutation; stale/hurt may create reviewable anti-memory candidates only. | implemented now | `pnpm eval:brain-battle:smoke` runs CLI memory record apply guards covering helped, stale, and hurt outcomes plus Memory Core non-mutation output. | `packages/cli/src/runMemoryRecordApplyCommand.ts`; `packages/cli/src/runCli.test.ts`; `docs/reviews/controlled-dogfood/2026-06-25-memory-feedback-demotion-loop/REPORT.md`; `docs/reviews/controlled-dogfood/2026-06-25-anti-memory-conflict-integration/REPORT.md`. | Does not prove feedback consistently improves future activation. |
| Memory promotion requires an untrusted-source review ref before external source lineage can cross into Memory Core. | implemented now | Focused memory review gate tests reject untrusted source lineage without `untrustedSourceReviewRef`; CLI tests pass `--untrusted-source-review-ref` through persisted promotion and preserve the review ref in promotion metadata. | `packages/harness/src/memory/memoryReviewGate.ts`; `packages/harness/src/memory/memoryReviewGate.test.ts`; `packages/cli/src/runMemoryCandidateReviewCommand.ts`; `packages/cli/src/runCli.test.ts`; `docs/architecture/security-trust-boundaries.md`. | Does not prove reviewer judgment is correct or poisoned source text is impossible. |
| Retained source decisions must name retrievable source location, trust tier, mechanism, KRN implication, decision, consumer, falsifier, and non-proof boundary before becoming KRN guidance. | implemented now | Focused source-map invariant test fails when a `docs/KRN_SOURCES.md` source section lacks `URL`/`URLs`, uses a non-`https://` source location, or lacks `Trust tier`, `Mechanism`, `KRN implication`, `Decision`, `Consumer`, `Falsifier`, or `Does not prove`. | `docs/KRN_SOURCES.md`; `packages/harness/src/sourceMapInvariants.test.ts`; `docs/runbooks/pattern-intake.md`. | Does not prove source coverage is complete, external docs are current, or trust-tier judgment is perfect. |
| Durable source guidance must classify source class and decision kind before shaping KRN behavior. | implemented now | Focused source-map invariant test fails when a `docs/KRN_SOURCES.md` retained source lacks `Source class` or `Decision kind`, preserving the pattern-intake distinction between official docs, courses, practitioner writing, papers, repo evidence, adoption, rejection, lab-test, and deferral. | `docs/KRN_SOURCES.md`; `docs/runbooks/pattern-intake.md`; `packages/harness/src/sourceMapInvariants.test.ts`. | Does not prove every future source is high quality, external pages are current, or adopted course/practitioner guidance is locally sufficient without falsifiers. |
| Brain-battle eval matrix implemented checks must name guard, evidence, and proof/non-proof boundary. | implemented now | Focused matrix invariant test parses `docs/architecture/brain-battle-eval-matrix.md` and fails when implemented rows lack substantive guard/evidence or any row lacks a non-proof boundary. | `docs/architecture/brain-battle-eval-matrix.md`; `packages/harness/src/brainBattleMatrixInvariants.test.ts`. | Does not prove the selected checks are strategically complete or that eval quality is sufficient. |
| Active context surfaces must keep raw materials and broad historical ledgers out of default resume context. | implemented now | Focused context hygiene invariant checks `AGENTS.md`/`docs/KRN_KERNEL.md` keep raw material quarantine and small-context rules, and fails if root `GOAL.md` or `PLAN.md` route active truth to `docs/materials/` or historical-ledger paths. | `AGENTS.md`; `docs/KRN_KERNEL.md`; `GOAL.md`; `PLAN.md`; `packages/harness/src/contextHygieneInvariants.test.ts`. | Does not prove every historical doc is clean or that raw materials are never useful for an explicitly scoped audit. |
| Active goal/plan surfaces must remain compact and not become completed-slice ledgers. | implemented now | Focused context hygiene invariant fails when root `GOAL.md` exceeds 130 lines, root `PLAN.md` exceeds 170 lines, or root active truth lists individual three-digit completed slices instead of keeping completed detail in `PLANS.md`. | `GOAL.md`; `PLAN.md`; `PLANS.md`; `packages/harness/src/contextHygieneInvariants.test.ts`. | Does not prove `PLANS.md` is small, historical evidence should be deleted, or product readiness. |
| Local verification TMPDIR overrides must stay outside the repo workspace. | implemented now | Focused active-plan invariant checks root `PLAN.md` verification policy keeps the temporary-directory write-error guidance, uses `/home/krn/.cache/krn-tmp`, and warns not to put `TMPDIR` under the repo checkout because CLI boundary tests rely on outside-workspace temporary directories. | `PLAN.md`; `packages/harness/src/activePlanInvariants.test.ts`; `packages/cli/src/cliFileBoundary.test.ts`. | Does not prove every local environment has enough temp space or that CI needs the override. |
| Stale pasted objectives, attachments, old prompts, and summaries must not roll active state backward. | implemented now | Focused active-plan invariant checks root `GOAL.md` says stale pasted objectives are historical evidence when they conflict with current root active state, checks `PLANS.md` records the stale-attachment objective guard, and checks the compact GOAL contract template preserves the same stale-objective rule for future compaction. | `GOAL.md`; `PLAN.md`; `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`; `/home/krn/.codex/attachments/7e4336bf-875e-413a-8fa4-489f4f11133a/pasted-text-1.txt`. | Does not prove every future external prompt is fresh or that stale attachments should be ignored before reading. |
| `PLANS.md` Progress must not retain stale unchecked active tasks after the compact active state has moved on. | implemented now | Focused active-plan invariant checks `PLANS.md` Progress for unchecked active task lines and explicitly rejects the stale V70/V86 active entries found after V150. | `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`; root `PLAN.md`. | Does not prove every historical progress entry is exhaustive or that progress should duplicate detailed outcomes. |
| Root `PLAN.md` must preserve the continuous pattern gate so compact-only resumes keep research/course/paper/source decisions bounded by consumer and falsifier. | implemented now | Focused active-plan invariant checks root `PLAN.md` contains the Pattern Gate section, the relevant pattern surfaces, and the source-to-decision chain through consumer and falsifier. | `PLAN.md`; `GOAL.md`; `PLANS.md`; `docs/runbooks/pattern-intake.md`; `packages/harness/src/activePlanInvariants.test.ts`. | Does not prove every future source is high quality, every future slice applies the gate correctly, or research creates product value by default. |
| GOAL, PLAN, and PLANS must point at the same active stream/task after compaction or resume, and that current task must not also be marked complete. | implemented now | Focused active-plan invariant test reads the root files, fails on active stream/current task divergence, and fails if compact active truth points at a completed task or stream. | `GOAL.md`; `PLAN.md`; `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`. | Does not prove the selected next task is strategically correct. |
| Detailed `PLANS.md` compact GOAL contract must not carry stale concrete active streams. | implemented now | Focused active-plan invariant checks section 22 uses current-state placeholders instead of hard-coded `V...` active stream/task examples. | `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`; `GOAL.md`; `PLAN.md`. | Does not prove all historical examples in `PLANS.md` are current or that the selected next task is correct. |
| Detailed `PLANS.md` compact GOAL contract must preserve the continuous pattern gate for future compaction. | implemented now | Focused active-plan invariant checks section 22 keeps the non-trivial pattern surface rule and the source-to-decision chain through consumer and falsifier. | `PLANS.md`; `GOAL.md`; `PLAN.md`; `packages/harness/src/activePlanInvariants.test.ts`; `docs/runbooks/pattern-intake.md`. | Does not prove every future compact goal is regenerated correctly or every future slice applies the gate. |
| Root `GOAL.md` continuation research guidance must include a consumer before falsifier. | implemented now | Focused active-plan invariant checks the `Continuation After Compact` section keeps the full source-to-decision chain through consumer and falsifier and rejects the older no-consumer chain. | `GOAL.md`; `PLAN.md`; `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`; `docs/runbooks/pattern-intake.md`. | Does not prove future runs apply the gate correctly or every source has a useful consumer. |
| Kernel decision rule must include a consumer before falsifier. | implemented now | Focused active-plan invariant checks `docs/KRN_KERNEL.md` preserves the full source-to-decision chain through consumer and falsifier. | `docs/KRN_KERNEL.md`; `GOAL.md`; `PLAN.md`; `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`. | Does not prove every future source has a useful consumer or every future run applies the gate. |
| `PLANS.md` known current gap must point to the current task instead of stale historical gaps. | implemented now | Focused active-plan invariant checks the top `Known current gap` section contains the current task and rejects the stale V63 current-gap wording. | `PLANS.md`; `GOAL.md`; `PLAN.md`; `packages/harness/src/activePlanInvariants.test.ts`. | Does not prove every historical gap in the ledger is current or that the selected next task is strategically correct. |
| `PLANS.md` revision note must remain historical and not claim an old next active stream. | implemented now | Focused active-plan invariant checks section 23 is framed as creation-time history and rejects `sets V... next active stream` wording. | `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`; `GOAL.md`; `PLAN.md`. | Does not prove every historical note is current or that old V05 history should be deleted. |
| Active execution surfaces must preserve the continuous pattern gate for infra, harness, CI/eval, Codex surfaces, TypeScript, target workflow, and research/course-driven work. | implemented now | Focused active-plan invariant checks root `GOAL.md` keeps the pattern-surface rule and source-to-decision chain, and `PLANS.md` keeps the pattern intake runbook and Surface Consumer Matrix. | `GOAL.md`; `PLANS.md`; `docs/runbooks/pattern-intake.md`; `packages/harness/src/activePlanInvariants.test.ts`. | Does not prove every future slice applies the gate correctly, every source is high quality, or research creates product value by default. |
| Pattern intake for courses, papers, docs, local evidence, and practitioner sources must end in a decision/rejection, reviewable candidate output, next action, or explicit rejection reason. | implemented now | Focused active-plan invariant checks `docs/runbooks/pattern-intake.md` keeps legal/content boundaries, source-to-decision chain, output template fields for `decision_kind`, `source_class`, `candidate_output.reviewability`, `next_action`, and rejection reasons for decorative/no-consumer/no-falsifier/copyright/stale/too-broad sources. | `docs/runbooks/pattern-intake.md`; `packages/harness/src/activePlanInvariants.test.ts`; `docs/KRN_SOURCES.md`. | Does not prove every future source intake is high quality, external docs are current, or course/paper guidance should be adopted without local falsifiers. |
| Source-class vocabulary must stay aligned across pattern intake, source-to-decision skill, durable source map, and source-map invariant. | implemented now | Focused active-plan invariant checks the source-class vocabulary is present in `docs/runbooks/pattern-intake.md`, `.agents/skills/source-to-decision/SKILL.md`, `docs/KRN_SOURCES.md`, and `packages/harness/src/sourceMapInvariants.test.ts`. | `docs/runbooks/pattern-intake.md`; `.agents/skills/source-to-decision/SKILL.md`; `docs/KRN_SOURCES.md`; `packages/harness/src/sourceMapInvariants.test.ts`; `packages/harness/src/activePlanInvariants.test.ts`. | Does not prove every source class is complete forever, every future source is high quality, or source classification alone creates product value. |
| Latest continuous `PLANS.md` outcome must preserve a reviewable source-to-decision record. | implemented now | Focused active-plan invariant checks the latest outcome before the final-response contract keeps `Source-to-decision` plus Source, Mechanism, KRN implication, Decision, Does not prove, Consumer, and Falsifier fields. | `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`; `.agents/skills/source-to-decision/SKILL.md`. | Does not prove the source judgment is correct, every historical outcome is complete, or research creates product value by default. |
| Future generated tasks must keep explicit Codex continuation contracts instead of vague backlog notes. | implemented now | Focused active-plan invariant checks `PLANS.md` keeps the Task Contract Schema and Generated Task Backlog template fields for allowed/forbidden writes, outputs, verification, acceptance, rollback, condensation, next-task synthesis, consumer, falsifier, and non-proof boundaries. | `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`; `docs/KRN_SOURCES.md#codex-prompting-guide`. | Does not prove every historical task entry follows the schema or that future task selection is strategically correct. |
| Final responses after Codex runs must preserve proof/non-proof, command, CI, condensation, next-task, and blocker fields. | implemented now | Focused active-plan invariant checks `PLANS.md` section 21 keeps the required final response fields for DB usage, commands, artifacts, commits/CI, proof/non-proof, condensation decisions, tasks appended, next active task, and blocked/budget status. | `PLANS.md`; `packages/harness/src/activePlanInvariants.test.ts`; `.agents/skills/handoff-compact/SKILL.md`. | Does not prove every future assistant response follows the contract or that the next active task is strategically correct. |
| Repo-local skills must stay routable and verifiable instead of becoming hidden prompt ledgers. | implemented now | Focused skill invariant test checks skill name/directory alignment, description routing, Workflow section, and Verification section; it caught and repaired missing sections in `target-repo-testing`. | `.agents/skills/*/SKILL.md`; `packages/harness/src/skillInvariants.test.ts`; `.agents/skills/target-repo-testing/SKILL.md`. | Does not prove every skill is complete or that more skills are useful. |
| Source-to-decision skill must keep research intake routed through pattern intake, legal boundaries, consumers, falsifiers, source class, decision kind, and reviewable candidate outputs. | implemented now | Focused skill invariant checks `.agents/skills/source-to-decision/SKILL.md` keeps the pattern-intake runbook reference, anti-decoration rules, paid/proprietary course boundary, source-hoarding ban, consumer/falsifier gate, required output fields, `source_class`, `decision_kind`, and candidate reviewability labels. | `.agents/skills/source-to-decision/SKILL.md`; `docs/runbooks/pattern-intake.md`; `packages/harness/src/skillInvariants.test.ts`; `docs/KRN_SOURCES.md`. | Does not prove every future Codex run invokes the skill, every source is high quality, or research creates product value by default. |
| Promptfoo must remain a bounded integration smoke/result adapter and must not become KRN behavior proof authority. | implemented now | Focused matrix invariant checks the package Promptfoo smoke script, fixture, provider, and boundary doc preserve local JSONL output, `doesNotExecuteKrnBehavior=true`, `promptfoo_integration_smoke`, runner/config/provider/result mapping language, and the rule that only `krn_behavior_execution` satisfies GoldenTask behavior proof. | `package.json`; `tests/fixtures/promptfoo/krn-golden-smoke.yaml`; `tests/fixtures/promptfoo/krn-golden-smoke-provider.mjs`; `docs/architecture/promptfoo-adapter-boundary.md`; `packages/harness/src/brainBattleMatrixInvariants.test.ts`. | Does not prove Promptfoo executes KRN behavior, Memory Brain readiness, eval quality, or product readiness. |
| Handoff compact must preserve active task, verified state, decisions, context selectors, next action, and what not to reread. | implemented now | Focused skill invariant test fails if `handoff-compact` loses its active `PLANS.md` stream/task routing, verified commit/push/CI state, first-incomplete-task rule, or required output fields. | `.agents/skills/handoff-compact/SKILL.md`; `packages/harness/src/skillInvariants.test.ts`; `GOAL.md`; `PLAN.md`; `PLANS.md`. | Does not prove Codex will always invoke the skill or that every future handoff is complete. |
| Anti-memory blocks or warns against stale/unsafe context during activation. | implemented now | Existing GoldenGate anti-memory case executes activation conflict behavior. | `packages/harness/src/goldenKrnBehaviorGate.ts`; `packages/harness/src/goldenKrnBehaviorGate.test.ts`. | Does not prove anti-memory coverage is complete for all stale claims. |
| Run readback distinguishes proof from non-proof and can be used by operator or Promptfoo without ad hoc SQL. | implemented now | `pnpm eval:brain-battle:smoke` runs the CLI readback proof/non-proof guard. | `packages/cli/src/runRunShowCommand.ts`; `packages/cli/src/runRunShowCommand.test.ts`; `docs/reviews/controlled-dogfood/2026-06-25-run-evidence-readback/REPORT.md`. | Does not prove live DB readback unless DB commands run. |
| Eval, golden behavior, protected failure mode, and policy gate IDs should not be type-compatible with each other. | implemented now | Core ID typecheck proof brands `PolicyGateId`, `EvalCandidateId`, `GoldenTaskId`, `GoldenCaseId`, and `ProtectedFailureModeId` and verifies cross-authority separation while preserving string compatibility. | `packages/core/src/ids.ts`; `packages/core/src/ids.typecheck.ts`; `packages/core/src/goldenTask.ts`; `packages/core/src/policy.ts`; `packages/core/src/eval.ts`; `docs/KRN_SOURCES.md#designing-your-types`. | Does not prove eval quality, policy judgment, runtime ID changes, or product readiness. |
| V02-01 cannot be satisfied by simulation. | requires external operator evidence | Root `PLAN.md` and `GOAL.md` keep V02-01 blocked/deferred until real operator setup exists. | `docs/runbooks/second-operator-alpha-trial.md`; `docs/reviews/controlled-dogfood/2026-06-25-second-operator-trial-packet/REPORT.md`; root `PLAN.md`. | Does not complete V02-01. |
| LLM-as-judge grades broad brain quality. | rejected as eval theater | None. LLM judging is non-blocking until validated against known deterministic cases. | Promptfoo remains an adapter boundary. | Would not prove KRN product readiness. |
| Broad dashboard/API/MCP eval suite. | rejected as eval theater | None. | Current product gates reject new product surfaces before behavior proof. | Would add surface area before the brain behavior is proven. |

## Current Smoke

Run:

```sh
pnpm eval:brain-battle:smoke
```

This executes the existing KRN GoldenGate behavior test plus the V02-02 target
trust exclusion case, the V03-01 target fixture battle case, the V03-02 target
owner-file-below-roots case, the V02-03 decorative source rejection case, the
V02-03 CLI run-readback proof/non-proof guard, the V02-04 CLI memory feedback /
anti-memory candidate guards, and the V02-05 Codex brief / ContextROI guards.
It also runs focused harness invariants for active plan freshness, active
context hygiene, source-map source-to-decision mapping, source location scheme,
pattern-intake output contract, repo-local skill routability,
source-to-decision skill contract, root PLAN pattern-gate visibility,
TypeScript boundary hygiene, and matrix guard/proof boundaries. It is
deterministic and does not call an LLM judge.

## Promptfoo Boundary

Promptfoo remains a portable integration smoke and result adapter, not behavior
proof. The existing Promptfoo smoke proves the runner/config/provider path and
local JSONL artifact output only. The fixture/provider must continue to state
`doesNotExecuteKrnBehavior=true`, and GoldenTask behavior proof still requires
`krn_behavior_execution`.

## Next Candidate

If future dogfood again shows review burden around run readback or source
decorative retention, promote one focused deterministic guard rather than
building a broad eval platform.
